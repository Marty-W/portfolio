---
interface Props {
  children: any
  title?: string
  className?: string
  pattern: 
}

const { title, className } = Astro.props
---

<div
  class:list={[
    "space-y-4 rounded-lg bg-neutral-100 p-3 shadow-inner",
    className,
  ]}
>
  {title && <h3 class="font-display text-2xl">{title}</h3>}
  <div class="text-neutral-600">
    <slot />
  </div>
</div>
